{% schema %}
{
  "name": "t:sections.spacing.name",
  "tag": "section",
  "settings": [
    {
      "type": "header",
      "content": "t:sections.global.header.settings.content",
      "info": "t:sections.global.header.settings.info"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "default": "scheme-3",
      "label": "t:sections.global.color_scheme.label"
    },
    {
      "type": "select",
      "id": "spacing_top",
      "label": "t:sections.global.settings.spacing.settings.spacing_top.label",
      "options": [
        {
          "value": "0",
          "label": "No"
        },
        {
          "value": "1",
          "label": "S"
        },
        {
          "value": "2",
          "label": "M"
        },
        {
          "value": "4",
          "label": "L"
        },
        {
          "value": "6",
          "label": "XL"
        }
      ],
      "default": "2"
    },
    {
      "type": "select",
      "id": "spacing_bottom",
      "label": "t:sections.global.settings.spacing.settings.spacing_bottom.label",
      "options": [
        {
          "value": "0",
          "label": "No"
        },
        {
          "value": "1",
          "label": "S"
        },
        {
          "value": "2",
          "label": "M"
        },
        {
          "value": "4",
          "label": "L"
        },
        {
          "value": "6",
          "label": "XL"
        }
      ],
      "default": "2"
    }
  ],
  "presets": [
    {
      "name": "t:sections.spacing.name"
    }
  ]
}
{% endschema %}

{%- assign s3_sid = section.id -%}
{%- assign s3_top = section.settings.spacing_top -%}
{%- assign s3_bot = section.settings.spacing_bottom -%}
{%- assign s3_scheme = section.settings.color_scheme -%}

{%- style -%}
  #shopify-section-{{ s3_sid }} > div {
    padding-block-start: calc(var(--section-spacing-unit-size) * {{ s3_top }});
    padding-block-end: calc(var(--section-spacing-unit-size) * {{ s3_bot }});
  }
{%- endstyle -%}

<div class="color-{{ s3_scheme }} gradient">
  <div class="container max-w-full block" aria-hidden="true"></div>
</div>
