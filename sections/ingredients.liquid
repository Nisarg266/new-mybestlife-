{% if section.blocks.size > 0 %}
  <style>
    .clinical-ingredients-{{ section.id }} {
      background: {{ section.settings.background_color }};
      padding: 60px 0;
      text-align: center;
    }

    .container-ingredients {
      max-width: var(--max-width);
      margin: 0 auto;
      padding: 0 20px;
    }

    .clinical-ingredients-{{ section.id }} .section__heading {
      font-size: clamp(30px, 5vw, 50px) !important;
      text-transform:Uppercase;
      font-weight: 700;
      margin-bottom: 50px;
      color: #b4975a;
      text-align: center;
    }

    .ingredients__grid-{{ section.id }} {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 30px;
      width: 100%;
      max-width: 1600px;
      margin: 0 auto;
    }

    .ingredient-item-{{ section.id }} {
      background:#ECE9E6;
      border-radius: 16px;
      padding: 40px 30px;
      text-align: left;
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
      height: 100%;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
    }

    .ingredient-item-{{ section.id }}:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
    }

    .ingredient-img-{{ section.id }} {
        position: absolute;
    top: 0px;
    right: -14px;
    width: 50%;
    height: 100%;
    }

    .ingredient-img-{{ section.id }} img {
      width: 100%;
      height: 100%;
      object-fit: cover ;
    }

    .ingredient-title-{{ section.id }} {
font-size: clamp(20px, 5vw, 30px) !important;
      font-weight: 700;
      line-height: 1.3;
      color: #231f20;

    }

    .ingredient-description-{{ section.id }} {
      font-size: 16px;
      font-weight: 400;
      line-height: 1.6;
      color: #666;
    margin-bottom: 60px;      flex-grow: 1;
    }

    .ingredient-button-{{ section.id }} {
      font-weight: 600;
      font-size: 14px;
      color: rgb(0, 0, 0);
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 6px;
      transition: all 0.3s ease;
      position: relative;
    }
.read-more-v{
padding-left:20px;
}
  .ingredient-button-{{ section.id }} {
  display: inline-flex;
  align-items: center;
  gap: 10px; /* space between arrow circle and text */
  font-weight: 600;
  font-size: 14px;
  color: #b4975a; /* gold text */
  text-decoration: none;
  transition: all 0.3s ease;
}

/* Remove the existing after arrow */
.ingredient-button-{{ section.id }}::after {
  content: none !important;
}

/* Circle around arrow */
.ingredient-button-{{ section.id }} .arrow-circle {
  background-color: #2a2a2a;
  border-radius: 50%;
  width: 28px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

/* Arrow inside circle */
.ingredient-button-{{ section.id }} .arrow-circle svg {
  width: 12px;
  height: 12px;
  stroke: white;
  fill: none;
  stroke-width: 2;
}


    .ingredient-text-{{ section.id }} {
      padding:60px 20px;
      padding-bottom:0px;
      width:50%;
      display: flex;
      flex-direction: column;
      flex: 1;padding-bottom: 0px;
width: 50%;
display: flex;
flex-direction: column;
flex: 1;
justify-content: center;  /* vertical centering */
    }

    .custom-swiper-arrow-{{ section.id }} {
      width: 48px;
      height: 48px;
      background-color:rgb(0, 0, 0);
      border-radius: 50%;
      position: absolute;
      top: 50%;
      transform: translateY(-50%);
      z-index: 11;
      display: flex !important;
      align-items: center;
      justify-content: center;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    .swiper-button-prev-{{ section.id }} {
      left: 10px;
    }

    .swiper-button-next-{{ section.id }} {
      right: 10px;
    }

    .swiper-button-prev-{{ section.id }} svg {
      transform: rotate(180deg);
    }

    .swiper-button-prev-{{ section.id }}::after,
    .swiper-button-next-{{ section.id }}::after {
      display: none;
    }
/* Arrow animation inside ingredient buttons */
.ingredient-button-{{ section.id }} .arrow-circle svg {
  transition: transform 0.4s ease-in-out, opacity 0.4s ease-in-out;
}

.ingredient-item-{{ section.id }}:hover .ingredient-button-{{ section.id }} .arrow-circle svg {
  animation: arrow-loop-{{ section.id }} 0.6s ease-in-out forwards;
}

@keyframes arrow-loop-{{ section.id }} {
  0% {
    transform: translateX(0);
    opacity: 1;
  }
  40% {
    transform: translateX(8px);
    opacity: 0;
  }
  41% {
    transform: translateX(-8px);
    opacity: 0;
  }
  100% {
    transform: translateX(0);
    opacity: 1;
  }
}

    /* Tablet Styles */
    @media (max-width: 989px) and (min-width: 769px) {
      .ingredients__grid-{{ section.id }} {
        gap: 24px;
      }

      .ingredient-item-{{ section.id }} {
        padding: 30px 24px;
      }

      .ingredient-title-{{ section.id }} {
        font-size: 22px;
      }

      .ingredient-description-{{ section.id }} {
        font-size: 15px;
      }
    }

    /* Mobile Styles */
    @media (max-width: 768px) {
.container-ingredients{
      padding: 0 0px;

}
      .ingredient-button-{{ section.id }} .arrow-circle {
  width: 25px;
  height: 25px;
}

      .clinical-ingredients-{{ section.id }} {
        padding: 40px 0;
      }

      .clinical-ingredients-{{ section.id }} .section__heading {
        font-size: 28px;
        margin-bottom: 30px;
      }

      .ingredient-title-{{ section.id }} {
        font-size: 20px;
        text-align: left;
        padding-right: 0;
        margin-bottom: 12px;
      }

      .ingredient-description-{{ section.id }} {
        font-size: 14px !important;
        text-align: left;
        margin-bottom: 20px;
      }

      .ingredient-text-{{ section.id }} {
        align-items: flex-start;
        text-align: left;
            padding: 60px 0px;
      }

      .ingredient-item-{{ section.id }} {
        padding: 24px 20px;
        padding-bottom: 30px;
      }

      .ingredient-img-{{ section.id }} {

      }

      .ingredient-button-{{ section.id }} {
        font-size: 13px !important;
      }

      .ingredients__grid-{{ section.id }} {
        display: none !important;
      }

      .ma-ingredients-swiper-{{ section.id }} {
        display: block;
        position: relative;
        padding: 0 40px;
      }

      .swiper-slide {
        flex: 0 0 auto;
        width: 100%;
      }

      .custom-swiper-arrow-{{ section.id }} {
        width: 36px;
        height: 36px;
      }

      .swiper-button-prev-{{ section.id }} {
        left: 0;
      }

      .swiper-button-next-{{ section.id }} {
        right: 0;
      }
    }

    @media (min-width: 769px) {
      .ma-ingredients-swiper-{{ section.id }},
      .custom-swiper-arrow-{{ section.id }} {
        display: none !important;
      }
    }
  </style>

  <div class="clinical-ingredients-{{ section.id }} color-{{ section.settings.color_scheme }} gradient js-animation-fade-in section-{{ section.id }}-padding">
    <div class="container-ingredients">
      {% if section.settings.heading_line1 != blank or section.settings.heading_line2 != blank %}
        <h2 class="section__heading">
          {{ section.settings.heading_line1 }}
          {% if section.settings.heading_line2 != blank %}
            <br>{{ section.settings.heading_line2 }}
          {% endif %}
        </h2>
      {% endif %}

      {% if section.settings.showslider_mobile %}
        <div class="swiper ma-ingredients-swiper-{{ section.id }} d-block d-md-none">
          <div class="swiper-wrapper">
            {% for block in section.blocks %}
              <div class="ingredient-item-{{ section.id }} swiper-slide" {{ block.shopify_attributes }}>
                {% if block.settings.image %}
                  <div class="ingredient-img-{{ section.id }}">
                    <img src="{{ block.settings.image | img_url: 'master' }}" alt="{{ block.settings.heading | escape }}" loading="lazy">
                  </div>
                {% endif %}
                <div class="ingredient-text-{{ section.id }}">
                  <h3 class="ingredient-title-{{ section.id }}">{{ block.settings.heading }}</h3>
                  <p class="ingredient-description-{{ section.id }}">{{ block.settings.text }}</p>
                  {% if block.settings.button_text and block.settings.button_link %}
  <a href="{{ block.settings.button_link }}" class="ingredient-button-{{ section.id }}">
    <span class="arrow-circle">
      <svg fill="#000000" viewBox="0 0 32 32" xmlns="http://www.w3.org/2000/svg">
        <path d="M5.975 17.504l14.287.001-6.367 6.366L16.021 26l10.004-10.003L16.029 6l-2.128 2.129 6.367 6.366H5.977z"></path>
      </svg>
    </span>
    {{ block.settings.button_text }}
  </a>
{% endif %}
                </div>
              </div>
            {% endfor %}
          </div>

          {% if section.settings.pagination %}
            <div class="swiper-button-prev-{{ section.id }} custom-swiper-arrow-{{ section.id }}">
              <!-- Prev Arrow SVG -->
              <svg xmlns="http://www.w3.org/2000/svg" width="7" height="12"><path d="M0.285376 10.7726C0.0408089 11.0167 0.0463463 11.4079 0.298297 11.6457C0.549324 11.8835 0.951706 11.8782 1.19627 11.6332L6.09869 6.71646L5.64371 6.28662L6.10054 6.71736C6.34511 6.47148 6.33957 6.07933 6.0867 5.84152L1.19535 0.981335C0.950784 0.736351 0.549324 0.730967 0.297374 0.968772C0.0463463 1.20658 0.0408089 1.59693 0.285376 1.84192L4.75773 6.28841L0.285376 10.7726Z" fill="white"/></svg>
            </div>
            <div class="swiper-button-next-{{ section.id }} custom-swiper-arrow-{{ section.id }}">
              <!-- Next Arrow SVG -->
              <svg xmlns="http://www.w3.org/2000/svg" width="7" height="12"><path d="M0.285376 10.7726C0.0408089 11.0167 0.0463463 11.4079 0.298297 11.6457C0.549324 11.8835 0.951706 11.8782 1.19627 11.6332L6.09869 6.71646L5.64371 6.28662L6.10054 6.71736C6.34511 6.47148 6.33957 6.07933 6.0867 5.84152L1.19535 0.981335C0.950784 0.736351 0.549324 0.730967 0.297374 0.968772C0.0463463 1.20658 0.0408089 1.59693 0.285376 1.84192L4.75773 6.28841L0.285376 10.7726Z" fill="white"/></svg>
            </div>
          {% endif %}
        </div>
      {% endif %}

      <div class="ingredients__grid-{{ section.id }}">
        {% for block in section.blocks %}
          <div class="ingredient-item-{{ section.id }}" {{ block.shopify_attributes }}>
            {% if block.settings.image %}
              <div class="ingredient-img-{{ section.id }}">
                <img src="{{ block.settings.image | img_url: 'master' }}" alt="{{ block.settings.heading | escape }}" loading="lazy">
              </div>
            {% endif %}
            <div class="ingredient-text-{{ section.id }}">
              <h3 class="ingredient-title-{{ section.id }}">{{ block.settings.heading }}</h3>
              <p class="ingredient-description-{{ section.id }}">{{ block.settings.text }}</p>
              
            </div>

            <div class = "read-more-v">
              {% if block.settings.button_text and block.settings.button_link %}
<a href="{{ block.settings.button_link }}" class="ingredient-button-{{ section.id }}">
  <span class="arrow-circle">
   <svg fill="#000000" viewBox="0 0 32 32" xmlns="http://www.w3.org/2000/svg"><g id="SVGRepo_bgCarrier" stroke-width="0"></g><g id="SVGRepo_tracerCarrier" stroke-linecap="round" stroke-linejoin="round"></g><g id="SVGRepo_iconCarrier"><path d="M5.975 17.504l14.287.001-6.367 6.366L16.021 26l10.004-10.003L16.029 6l-2.128 2.129 6.367 6.366H5.977z"></path></g></svg>
  </span>
  {{ block.settings.button_text }}
</a>

              {% endif %}
            </div>
          </div>
        {% endfor %}
      </div>
    </div>
  </div>

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@9/swiper-bundle.min.css" />

<script>
  document.addEventListener('DOMContentLoaded', function () {
    const sectionId = '{{ section.id }}';
    const swiperSelector = `.ma-ingredients-swiper-${sectionId}`;
    const swiperContainer = document.querySelector(swiperSelector);

    if (!swiperContainer || swiperContainer.classList.contains('swiper-initialized')) return;

    swiperContainer.classList.add('swiper-initialized');

    // Add slide class (for safety)
    const wrapper = swiperContainer.querySelector('.swiper-wrapper');
    if (wrapper) {
      const slides = wrapper.children;
      Array.from(slides).forEach(slide => slide.classList.add('swiper-slide'));
    }

    // Force breakpoint init using `modules` logic
    let swiperInstance;

    function initSwiper() {
      swiperInstance = new Swiper(swiperSelector, {
        loop: false,
        spaceBetween: 15,
        slidesPerView: 1,
        navigation: {
          nextEl: `.swiper-button-next-${sectionId}`,
          prevEl: `.swiper-button-prev-${sectionId}`
        },
        breakpoints: {
          0: {
            slidesPerView: 1,
            allowTouchMove: true,
            enabled: true
          },
          769: {
            slidesPerView: 1,
            allowTouchMove: false,
            enabled: false
          }
        }
      });
    }

    // Load Swiper script if not loaded yet
    if (window.Swiper) {
      initSwiper();
    } else {
      const script = document.createElement('script');
      script.src = "https://cdn.jsdelivr.net/npm/swiper@9/swiper-bundle.min.js";
      script.onload = initSwiper;
      document.head.appendChild(script);
      
    }
  });
</script>



{% endif %}


{%- style -%}
.section-{{ section.id }}-padding {
  padding-top: {{ section.settings.top_padding | times: 0.75 | round: 0 }}px;
  padding-bottom: {{ section.settings.bottom_padding | times: 0.75 | round: 0 }}px;
  padding-left: {{ section.settings.left_padding | times: 0.75 | round: 0 }}px;
  padding-right: {{ section.settings.right_padding | times: 0.75 | round: 0 }}px;
}
@media (max-width: 768px) {
        .section-{{ section.id }}-padding {
    padding-top: 20px !important;
    padding-bottom: 20px !important;
    padding-left: 20px !important;
    padding-right: 20px !important;
  }
    }

{%- endstyle -%}

{% schema %}
{
  "name": "Clinically Ingredients",
  "tag": "section",
  "class": "section-clinical-ingredients",
  "settings": [
    {
      "type": "text",
      "id": "heading_line1",
      "label": "Heading - First Line",
      "default": "Innovative Ingredients"
    },
    {
      "type": "text",
      "id": "heading_line2",
      "label": "Heading - Second Line"},
    {
      "type": "color",
      "id": "background_color",
      "label": "Background Color",
      "default": "#FBE8E5"
    },
    {
      "type": "select",
      "id": "spacing_top",
      "label": "Spacing Top",
      "options": [
        { "value": "0", "label": "No" },
        { "value": "1", "label": "Small" },
        { "value": "2", "label": "Medium" },
        { "value": "4", "label": "Large" },
        { "value": "6", "label": "Extra Large" }
      ],
      "default": "0"
    },
    {
      "type": "select",
      "id": "spacing_bottom",
      "label": "Spacing Bottom",
      "options": [
        { "value": "0", "label": "No" },
        { "value": "1", "label": "Small" },
        { "value": "2", "label": "Medium" },
        { "value": "4", "label": "Large" },
        { "value": "6", "label": "Extra Large" }
      ],
      "default": "0"
    },
         {
      "type": "range",
      "id": "top_padding",
      "label": "Top padding",
      "min": 0,
      "max": 160,
      "step": 2,
      "unit": "px",
      "default": 92
    },
    {
      "type": "range",
      "id": "bottom_padding",
      "label": "Bottom padding",
      "min": 0,
      "max": 160,
      "step": 2,
      "unit": "px",
      "default": 92
    },
    {
  "type": "range",
  "id": "left_padding",
  "label": "Left padding",
  "min": 0,
  "max": 160,
  "step": 2,
  "unit": "px",
  "default": 20
},
{
  "type": "range",
  "id": "right_padding",
  "label": "Right padding",
  "min": 0,
  "max": 160,
  "step": 2,
  "unit": "px",
  "default": 20
},
        {
      "type": "checkbox",
      "id": "pagination",
      "label": "Pagination",
      "default": true
    },
    {
  "type": "checkbox",
  "id": "showslider_mobile",
  "label": "Enable Slider on Mobile",
  "default": true
},
     {
      "type": "range",
      "id": "slides_per_view_mobile",
      "label": "Slides Per View (Mobile)",
      "min": 1,
      "max": 5,
      "step": 0.1,
      "default": 2.2
    }],
  "blocks": [
    {
      "type": "ingredient",
      "name": "Ingredient Item",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Ingredient Title",
          "default": "Ingredient Title"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Description",
          "default": "This is ingredient description."
        },
        {
  "type": "text",
  "id": "button_text",
  "label": "Button Text",
  "default": "Read More"
},
{
  "type": "url",
  "id": "button_link",
  "label": "Button Link"
},
        {
          "type": "image_picker",
          "id": "image",
          "label": "Small Icon Image"
        },
        {
  "type": "range",
  "id": "block_padding_left",
  "label": "Block Padding Left",
  "min": 0,
  "max": 100,
  "step": 2,
  "unit": "px",
  "default": 0
},
{
  "type": "range",
  "id": "block_padding_right",
  "label": "Block Padding Right",
  "min": 0,
  "max": 100,
  "step": 2,
  "unit": "px",
  "default": 0
}

      ]
    }
  ],
  "max_blocks": 8,
  "presets": [
    {
      "name": "Clinically Ingredients",
      "blocks": []
    }
  ]
}
{% endschema %}