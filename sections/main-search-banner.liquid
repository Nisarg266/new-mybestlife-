{% schema %}
{
  "name": "t:sections.main-search-banner.name",
  "tag": "section",
  "settings": [
    {
      "type": "header",
      "content": "t:sections.global.header.settings.content",
      "info": "t:sections.global.header.settings.info"
    },
    {
      "type": "select",
      "id": "section_width",
      "options": [
        {
          "value": "max-w-page",
          "label": "t:sections.global.settings.section_width.options__1.label"
        },
        {
          "value": "max-w-fluid",
          "label": "t:sections.global.settings.section_width.options__2.label"
        }
      ],
      "default": "max-w-page",
      "label": "t:sections.global.settings.section_width.label"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "default": "scheme-1",
      "label": "t:sections.global.color_scheme.label"
    },
    {
      "type": "select",
      "id": "spacing_top",
      "label": "t:sections.global.settings.spacing.settings.spacing_top.label",
      "options": [
        {
          "value": "0",
          "label": "No"
        },
        {
          "value": "1",
          "label": "S"
        },
        {
          "value": "2",
          "label": "M"
        },
        {
          "value": "4",
          "label": "L"
        },
        {
          "value": "6",
          "label": "XL"
        }
      ],
      "default": "2"
    },
    {
      "type": "select",
      "id": "spacing_bottom",
      "label": "t:sections.global.settings.spacing.settings.spacing_bottom.label",
      "options": [
        {
          "value": "0",
          "label": "No"
        },
        {
          "value": "1",
          "label": "S"
        },
        {
          "value": "2",
          "label": "M"
        },
        {
          "value": "4",
          "label": "L"
        },
        {
          "value": "6",
          "label": "XL"
        }
      ],
      "default": "2"
    }
  ]
}
{% endschema %}

{{- 'component-search-hero.css' | asset_url | stylesheet_tag -}}

{%- assign s8_cfg = section.settings -%}
{%- assign s8_sid = section.id -%}

{%- style -%}
  #shopify-section-{{ s8_sid }} > div {
    padding-block-start: calc(var(--section-spacing-unit-size) * {{ s8_cfg.spacing_top }});
    padding-block-end: calc(var(--section-spacing-unit-size) * {{ s8_cfg.spacing_bottom }});
  }
{%- endstyle -%}

<div class="color-{{ s8_cfg.color_scheme }} gradient">
  <div class="search-hero">
    <div class="container {{ s8_cfg.section_width }}">
      <h1 class="search-hero__title">
        {%- if search.performed -%}
          <span class="search-hero__title-small h4">
            {{- 'search.results_for' | t -}}
          </span>
          <span>
            {{- search.terms -}}<small>{{- search.results_count -}}</small>
          </span>
        {%- else -%}
          {{- 'search.no_results' | t -}}
        {%- endif -%}
      </h1>
      <div class="search-hero__input">
        {%- render 'search', with input_id: 'desktop', input_class: ' js-drawer-focus-element', value: search.terms, clear_button_visible: true -%}
      </div>
    </div>
  </div>
</div>
